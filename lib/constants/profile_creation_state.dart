
/// When the user is creating a profile after signing up, this enum is used to
/// determine which step of the profile creation process the user is currently on.
enum ProfileCreationState {
  basicInfo,
  profilePicture,
}